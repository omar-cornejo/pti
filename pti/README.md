# PTI React + Express

## Instalación y ejecución

### Backend (Express)

```
cd backend
npm install
node src/index.js
```
El backend por defecto utiliza el puerto 3001.

### Frontend (React / Vite)

```
cd frontend
npm install
npm run dev
```
El frontend por defecto utiliza el puerto 5173.

---

Recuerda definir las variables de entorno y la configuración de la base de datos más adelante.
